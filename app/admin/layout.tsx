import { AdminAuthProvider } from "@/components/provider/AdminAuth";
import React from "react";

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default Layout;
